server {
    listen       80;
    server_name  hazime.com.br;
    root ~/nginx/hazime.com.br
    error_page  404 /index.html;

    location = / {
        try_files $uri $uri/ /index.html;
    }

    location = /about-us {
        try_files $uri $uri/ /index.html;
    }

    location = /contact {
        try_files $uri $uri/ /index.html;
    }

    location ~ /products {
        try_files $uri $uri/ /index.html;
    }
}